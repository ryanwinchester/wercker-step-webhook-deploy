name: webhook-deploy
version: 0.2.1
description: Triggers webhook from a service that deploys that way.
keywords:
    - webhook
    - deploy
    - envoyer
    - forge
properties:
    url:
        type: string
        required: true

