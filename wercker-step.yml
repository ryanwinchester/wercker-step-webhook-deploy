name: webhook-deploy
version: 0.1.0
description: Triggers webhook from a service that deploys that way.
keywords:
    - envoyer
    - forge
    - laravel
    - deploy
    - webhook
properties:
    url:
        type: string
        required: true

